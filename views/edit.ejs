<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500  px-3 py-2 rounded-md " href="/logout">Logout</a>
        </div>
        <h5 class="mb-3 text-2xl">Edit Your Post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea  class="p-3 text-white outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md " placeholder="Whats on your mind?" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 text-black block outline-none rounded-md mt-2 text-sm" type="submit" value="Update Post">
        </form>
        
    </div>
</body>
</html>